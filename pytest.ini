[pytest]
testpaths = dev/tests
pythonpath = .
norecursedirs = .* *.egg _darcs build CVS dist node_modules venv {arch} upstream
addopts = -m "not browser" --timeout=60
markers =
    browser: drives a real headless Chrome on loopback; verification, excluded from the default run (select with -m browser)
